# Fortunia API connection (server-side — never use NEXT_PUBLIC_ for secrets)
# These are only accessible on the Next.js server, not in the browser.
FORTUNIA_API_URL=http://fortunia-api:8000
FORTUNIA_API_KEY=

# Dashboard settings
NODE_ENV=development
